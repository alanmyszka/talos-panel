POSTGRES_DB=talos_panel
POSTGRES_USER=talos_panel
POSTGRES_PASSWORD=change-me
# Optional: absolute installation directory. Defaults to the current repository directory.
BASE_DIR=/opt/talos-panel
MINECRAFT_NETWORK=talos-network
# Set to true only when the panel is served exclusively through HTTPS.
SECURE_COOKIES=false
